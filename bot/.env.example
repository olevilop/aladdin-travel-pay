# ─────────────────────────────────────────────────────────────────────────────
# Конфигурация Telegram-бота. Скопируй в `.env` и заполни.
#   cp .env.example .env
# На сервере этот файл создаёт скрипт deploy/setup-bot.sh.
# ─────────────────────────────────────────────────────────────────────────────

# Токен бота от @BotFather (выглядит как 123456789:AA...)
TELEGRAM_BOT_TOKEN=

# Адрес нашего backend (на том же сервере)
BACKEND_URL=http://127.0.0.1:3001

# Сервисный токен — ДОЛЖЕН совпадать с BOT_API_TOKEN из backend/.env
BOT_API_TOKEN=

# Кто может пользоваться ботом — Telegram id через запятую (узнать: команда /id боту).
# Если оставить пустым — бот примет файлы от кого угодно (НЕБЕЗОПАСНО).
ALLOWED_USER_IDS=

# Разрешённые групповые чаты — chat id через запятую (узнать: команда /id в самом чате;
# id группы отрицательный, напр. -1001234567890). В этих чатах бот реагирует на файлы.
# Грузит всё равно только от пользователей из ALLOWED_USER_IDS.
# Для работы в группах ОБЯЗАТЕЛЬНО выключить privacy mode у @BotFather:
#   /mybots → выбрать бота → Bot Settings → Group Privacy → Turn off
ALLOWED_CHAT_IDS=
